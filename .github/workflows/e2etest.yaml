name: End-to-end tests
run-name: End-to-end test for ${{ github.sha }} on ${{ github.ref_name }}

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version: '1.27'
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.21.0

      - name: Install oathtool
        run: |
          sudo rm -f /etc/apt/sources.list.d/*chrome* /etc/apt/sources.list.d/*google*
          sudo apt-get update -o Acquire::http::No-Cache=true -o Acquire::https::No-Cache=true || true
          sudo apt-get install -y oathtool

      - name: Boot the cluster with dependant backend systems
        run: |
          ./ui/tests/scripts/01-start-cluster.sh &
          sleep 30 # wait for cluster to boot up

      - name: Start the login ui
        run: |
          ./ui/tests/scripts/02-start-ui.sh
          curl --head --fail --retry-delay 2 --retry 100 --retry-connrefused --insecure http://localhost/ui/login

      - name: Register a client and boot its container
        run: |
          ./ui/tests/scripts/03-start-oidc-app.sh

      - name: Install Node dependencies
        run: cd ui && npm ci

      - name: Install Playwright Browser
        run: |
          sudo rm -f /etc/apt/sources.list.d/*chrome* /etc/apt/sources.list.d/*google*
          cd ui && npx playwright install --with-deps chromium

      - name: Run the tests
        run: make test-e2e

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: playwright-results
          path: ui/playwright-report
