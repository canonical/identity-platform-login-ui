name: OCI Image updated

on:
    workflow_dispatch:
        inputs:
            oci-image: 
                description: 'OCI image to use'
                required: true
                type: string

permissions:
    contents: write
    pull-requests: write

jobs:
    update:
        uses: canonical/identity-team/.github/workflows/oci-update-pr.yaml@15ae2e6fe85c1ed3ba8f301eecd68440b7cafdb1
        with: 
            oci-image: ${{ inputs.oci-image }}
            repository: canonical/identity-platform-login-ui-operator
        secrets:
            PAT_TOKEN: ${{ secrets.PAT_TOKEN }}