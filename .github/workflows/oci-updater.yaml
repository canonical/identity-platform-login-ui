name: OCI Image updated

on:
    workflow_dispatch:
        inputs:
            oci-image: 
                description: 'OCI image to use'
                required: true
                type: string

permissions:
    contents: write
    pull-requests: write

jobs:
    update:
        uses: canonical/identity-team/.github/workflows/oci-update-pr.yaml@3bcf7c8d36b4e51e5a65d27041389d7456c17b57
        with: 
            oci-image: ${{ inputs.oci-image }}
            repository: canonical/identity-platform-login-ui-operator
        secrets:
            PAT_TOKEN: ${{ secrets.PAT_TOKEN }}